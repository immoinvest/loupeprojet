# Deklic — Spécification technique

Stack : voir `adr/001-stack.md`. Ce document décrit la structure et les conventions ; il est mis à jour à chaque feature (Rule 8).

## Monorepo

```
loupeprojet/
├── package.json            workspaces: ["packages/*", "apps/*", "data"] ; scripts lint / format / typecheck / test / test:coverage / build
├── tsconfig.base.json      strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes, ES2022, bundler resolution
├── tsconfig.json           couvre les fichiers de config racine (ESLint type-checked)
├── eslint.config.js        typescript-eslint strict-type-checked + stylistic, no-console, max-lines 300, prettier
├── marque/                 identité de marque Deklic (ADR-005) : logos SVG, favicon, icônes, image de partage, palette, guide
├── .prettierrc             printWidth 100, singleQuote, trailingComma all, LF
├── vitest.config.ts        projets = packages/*, apps/*, data ; seuils 100 % moteur, capture, gestion, projets, worker, comptes, extension, data, modules de logique du web
├── .github/workflows/ci.yml  Node 22 : npm ci → lint → format:check → typecheck → test:coverage → build ; job e2e : Playwright Chromium (npm run test:e2e)
├── .github/workflows/referentiels.yml  cron mensuel + manuel : génère data/dist puis aws s3 sync vers R2 deklic-data
├── packages/moteur/        ← livré (feature moteur-calcul)
├── packages/capture/       ← livré (feature extension) : contrat de capture et moteur de règles, partagés par l'extension, le favori et le web
├── packages/gestion/       ← livré (features gerer-socle et quittances-fiches) : calcul pur de la gestion locative (schémas, loyers dus, statuts dont partiel, résumé du mois, contenu et numéro des quittances et reçus, baux, colocation)
├── packages/projets/       ← livré (feature sync-projets) : schémas des projets enregistrés, contrat de synchronisation, fusion de l'appareil
├── apps/web/               ← livré (socle, nouveau projet, hypothèses, onglets, lecture de la capture, bouton-favori, page /extension, garder, simulateur de prêt) ; e2e Playwright
├── apps/worker/            ← socle livré (feature worker-socle)
├── apps/comptes/           ← livré (feature comptes) : Better Auth sur Hono, servi par le worker Pages, D1
├── apps/extension/         ← livré (feature extension) : WebExtension MV3, règles par portail, build esbuild vers dist/chrome et dist/firefox
└── data/                   ← livré (feature referentiels) : pré-agrégation des référentiels publics
```

## `packages/moteur` (livré)

```
src/
├── index.ts                 API publique
├── calculer-projet.ts       calculerProjet(entree, { avecScenarios }) : Zod parse → règles → base complète (scénarios) ou partielle sans loyer (sections null, manques) → meta
├── calculer-base.ts         financement → fiscalité → revente → rendement → verdict (sans scénarios)
├── schema/                  bien, marche, hypotheses, projet (entrée) ; resultats (sortie, strictObject)
├── regles/                  types.ts, 2026-09.ts (toutes les constantes datées, aConfirmer, simplifications), index.ts
├── commun/                  arrondi, résolution par bissection, VAN, erreurs nommées
├── achat/                   prixRetenu (prix affiché × (1 − négociation), arrondi à l'euro), resumerAchat, tauxPourPrixRetenu
├── financement/             frais-acquisition (base = prix retenu − honoraires acquéreur), mensualite, amortissement (différés), taeg, effort (HCSF), ira
├── location/                loyerMensuelHc, loyerMensuelReference, defautsPourMode (valeurs de départ par type), vacanceSemaines, tauxProportionnel
├── cashflow/                recettes (5 types : loyers bruts, charges récupérées, vacance, nuitées, séjours), charges (par régime et par type), point mort, couverture, projection annuelle
├── fiscalite/               amortissements, deficits, interets, micro-bic, lmnp-reel (39 C), micro-foncier, nu-reel (ProjectionRegime), bilan (revente, impôt total, reste au total), index (4 régimes, meilleurAuTotal)
├── revente/                 valeur, plus-value (abattements, surtaxe, réintégration), par-regime (reventeDuRegime, reventeParRegime), index (calculerRevente = régime retenu)
├── rendement/               rendements, tri, enrichissement, index
├── verdict/                 feux (5), vigilance (codes financiers), index
├── estimation/              estimerPrix, confiance, travaux (estimerTravaux, recalerTravaux, choisirTravaux : barème `regles.travaux`, `achat.travauxChoix`)
├── visite/                  types, contexte (typeExploitation, prédicats, paramètres), base/ (75 questions sourcées, un fichier par catégorie), questions (questionsPourProjet), index
├── scenarios/               prix-cible (3 critères), predefinis (6 transformations), index (deltas)
└── exemples/t3-marseille.ts projet d'exemple
tests/                       un dossier par module + integration/ ; 204 tests ; couverture 100 % lignes/branches/fonctions
```

- TypeScript pur, dépendance runtime unique : Zod. Aucun accès réseau, DOM, date système ou aléatoire.
- Les règles sont toujours **injectées** en paramètre (`regles`), jamais importées dans un module de calcul.
- Fichiers ≤ 300 lignes (ESLint `max-lines`), fonctions courtes, nommage français métier.
- Montants en euros flottants, arrondi explicite à l'affichage (`arrondirEuro`, `arrondirTaux`) ; taux en décimal ; durées nommées (`dureeMois`, `annees`).
- Performance : ~35 ms pour un projet complet (6 scénarios + 3 prix cibles) ; ~5 ms sans scénarios.

## `apps/web` (socle livré)

Voir `architecture/web-socle.md`. React 19 + Vite 7 + Tailwind v4 (`@theme` = tokens ADR-004), React Router 7 déclaratif (`useRoutes`), stockage local Zod (`loupe.projets.v1`), textes des codes du moteur dans `src/textes/`, Vitest + Testing Library (jsdom). Cloudflare Pages : `wrangler.toml`, `public/_redirects`. Couverture 100 % exigée sur `stockage/`, `formatage/`, `textes/`, `compte/` ; les écrans sont couverts par des tests de rendu et de navigation (`AppEnMemoire`). Tests de bout en bout : `apps/web/e2e/` avec Playwright (Chromium, build de production servi par `vite preview` sur 127.0.0.1:5199, sélecteurs par rôle et libellé, contexte neuf par test), `npm run test:e2e` ; voir `architecture/e2e-playwright.md`. Appels au Worker : `src/enrichissement/` (client revalidé par Zod, lecture IA puis règles, enrichissement marché), fourni par le contexte `coque/ClientWorker.tsx` (hors ligne par défaut, donc jamais de réseau en test) ; voir `architecture/enrichissement-marche.md`.

Feature `hypotheses-financement` (fiches 01 et 03 du backlog) : voir `architecture/hypotheses-financement.md`. Moteur : `HypothesesSchema.revenusMensuels` facultatif, `tauxEffort` prend `revenusMensuels: number | null` (aucun effort ni signal sans revenus), `feuCouverture(tauxCouverture, regles)` avec `regles.verdict.couverture` (70 % / 100 %) à la place de `feuEffort`, axe `couverture` dans `ResultatsSchema`. Web : onglet `/projets/:id/financement` (`ecrans/Financement.tsx`, cartes `ecrans/financement/Cartes.tsx`, `TableauAnnuel.tsx`), grille éditable partagée `ecrans/hypotheses/GrilleHypotheses.tsx` (et `LignesHypotheses` en mode document, valeurs formatées par `hypotheses/lisible.ts`), registre `TOUS_LES_GROUPES` (`GROUPES` = cartes de Hypothèses ; `GROUPE_MARCHE` et `preparerDvf` supprimés, plafond d'encadrement dans `GROUPE_LOCATION`), `analyses/simulation-pret.ts` (schéma Zod local du contrat `#s=` de la fiche 08, `simulationDepuisResultats`, `lienSimulateurPret`, `decoderSimulation`), `stockage/base64url.ts` (encodage partagé avec `partage.ts`), `composants/ui.tsx` `LienBouton`, route `simulateur-pret` (page Bientôt), volet Financement dans `DocumentProjet`, textes `textes/financement.ts`. Formulaire Vérifier, `SaisieProjet` et `construireProjet` sans revenus.
Feature `liens-courts-domaine` : voir `architecture/liens-courts-domaine.md` et ADR-009. Origines du site dans `@loupe/capture/origines` ; table D1 `partage` (migration 0006) servie par `/api/partage` du worker des comptes ; lien compressé par `CompressionStream('deflate-raw')`, sans dépendance ; transfert entre domaines par fragment `#d=` (jamais envoyé au serveur), bascule derrière `DEKLIC_TRANSFERT=1`.

Feature `visite-questions` : voir `architecture/visite-questions.md`. `ProjetEnregistre.visite` (Zod, migration douce), logique pure `src/visite/` (réponses, progression, groupes ; couverture 100 %), écran `ecrans/Visite.tsx` et `ecrans/visite/`, carte `ecrans/rapport/Vigilance.tsx`.

Feature `garder` : voir `architecture/garder.md`. Route `/projets/:id/imprimer` hors coque, rendue sous `ModeDocument` (contexte `composants/document.tsx` : boutons masqués, explications dépliées, grilles à deux colonnes) avec `@media print` dans `index.css` ; partage par fragment d'URL (`stockage/partage.ts`, base64url + Zod, jamais d'exception) ; Comparer (`analyses/comparaison.ts`) ; Méthode générée depuis `obtenirRegles()` (`textes/methode*.ts`, défauts lus par `analyses/defauts.ts`). Couverture 100 % sur ces modules (globs `stockage`, `analyses`, `textes`).

Feature `rapport-cashflow` (fiche de backlog 10) : voir `architecture/rapport-cashflow.md`. Composant `Info` (`composants/info.tsx` : bouton ⓘ de 44 px, bulle `role="tooltip"` absolue sous l'icône, décalage calculé par `decalageBulle` pour tenir dans l'écran, ouverture au clic et au focus, fermeture Échap / clic dehors / perte du focus ; paragraphe en mode document ; pas de `popover` natif, absent de jsdom) ; `LienOnglet` (lien relatif vers un volet, nul en mode document) ; `TitreCarte` avec `info` à côté du `h2` et `action` à droite ; `Pourquoi` supprimé. Dérivés du Rapport dans `analyses/rapport.ts` (`cascadeAutofinancement`, `multipleSurApport`) ; explications chiffrées dans `textes/explications.ts` (fonctions `(r: Resultats) => string`, `EXPLICATIONS` fixes conservées pour Méthode). Tests : `tests/{info,rapport}.test.tsx`, `tests/{rapport-analyses,explications}.test.ts`, variantes de projet partagées dans `tests/projets.ts` ; e2e `e2e/rapport.spec.ts`.

Feature `responsive` : voir `architecture/responsive.md` et ADR-007. Mise en page mobile d'abord (Tailwind `sm` 640, `md` 768, `lg` 1 024, `xl` 1 280, `2xl` 1 536) : tiroir sous `lg` (`coque/menu.ts` : Échap, voile, `inert`, focus rendu), `composants/mise-en-page.tsx` (`Page`, `TitrePage`, `Chapo`, équivalents `print:`), `pointer-coarse:` pour les cibles de 44 px et les champs de 16 px. PWA : manifeste (`id`, `scope`, icônes `maskable`, raccourcis, `share_target` GET vers `/projets/nouveau`), `application/` (suivi de `beforeinstallprompt`, décision de partage natif), service worker IIFE (`vite.hors-ligne.config.ts` → `dist/sw.js`, cache `deklic-<empreinte de index.html>`, stratégies pures dans `hors-ligne/strategie.ts` : navigation en réseau d'abord, annonce partagée coque d'abord, fichiers construits cache d'abord, manifeste et icônes réseau d'abord, `/api/*` ignoré, seule une page HTML gardée comme coque). Couverture 100 % sur `application/` et `hors-ligne/` ; `sw/` prouvé par Playwright.

Feature `coque-fixe` : voir `architecture/coque-fixe.md`. Coque « application » : `AppLayout` en `h-dvh overflow-hidden` (grille `[var(--largeur-menu)_1fr]` à partir de `lg`, `--largeur-menu: 14rem` dans `index.css`), `main` seul conteneur qui défile (`coque/contenu.ts` : `scrollTop = 0` à chaque changement de `pathname`), barre latérale en trois zones (seules les sections Analyser et Gérer défilent, voir « Gestion locative »). En-tête de projet `sticky` compact ; `coque/entete.ts` publie par `ResizeObserver` les variables `--decalage-entete` (début de la bande des volets, `top` négatif sous `md`) et `--hauteur-entete-projet` (partie en vue, `top` de la synthèse d'Hypothèses) sur le cadre `[data-cadre-projet]` ; sans observateur, rien n'est publié (défauts `0px`). La spec des formats (`e2e/formats.ts`) mesure le débordement de `main` et ne l'excuse pas comme conteneur qui défile.

Feature `accueil-menu` : voir `architecture/accueil-menu.md`. Route index = `ecrans/Accueil.tsx` (plus de redirection vers `/projets`), blocs `ecrans/accueil/{BlocAnalyser,BlocGerer}.tsx` ; règles pures `src/accueil/analyser.ts` (`estExempleIntact` : nom `NOM_PROJET_EXEMPLE` et `creeLe === modifieLe` ; `resumeAnalyser(projets, { gerer, cashflow })`, calcul du moteur injecté) ; textes `textes/accueil.ts` (`TEXTES_LOGO` = nom accessible des logos). Menu : `PROJETS_DANS_LE_MENU = 3`, ligne « Mes projets · N [+] » toujours présente en tête (feature `coque-menus` : `coque/LigneAvecAjout.tsx`, aussi « Mes biens · N [+] » dans Gérer ; statut par `composants/MenuChoix.tsx`, motif WAI-ARIA listbox, logique pure `composants/menu-choix.ts`, voir `architecture/coque-menus.md`), lien « Accueil » (`nav` « Accueil ») hors de la zone qui défile ; `gestion/menu.ts` : `CHOIX_MENU`, `preferencesDe`, `choixDe` (remplacent `basculer` et `estFigee`), `MonMenu` en groupe radio. `cheminDeRetour` vaut `/` par défaut.

Feature `formulaire-rapide` (fiches de backlog 13 et 20) : voir `architecture/formulaire-rapide.md`. Composants de saisie réutilisables `composants/saisie/` : `Tuiles<V>` (radios natives, re-clic = inconnu si `effacable` ; `SelecteurMode` bâti dessus), `Compteur` (pas fixe ou `pasAdaptatif`, saisie vidable), `EchelleEnergie` (DPE / GES, tokens `--color-dpe-*`, `--color-ges-*`), `ChampMontant` (milliers pendant la frappe, curseur de texte replacé), `Combobox<T>` (motif WAI-ARIA à liste, source `chercher(texte, signal)` injectée, anti-rebond 250 ms, `AbortController`, `choixAutomatique`), `ChampCommune` ; logique pure `montant.ts`, `pas.ts`, `energie.ts`, `liste.ts` (100 %). `Curseur` accepte `valeur: null` (`texteSansValeur`). Formulaire : `verifier/` pur (100 %) — `items.ts` (unités d'affichage, cartes, ordre d'influence, `TERMES_VERIFIER`), `groupes.ts` (`grouperChamps` figé à l'ouverture, masquage maison / RDC, `sansValeursMasquees`, `provenanceEnvoyee`, résumés), `deductions.ts` (chambres estimées, tuile de l'apport), `periodes.ts` (périodes depuis `obtenirRegles().visite`) ; écran `ecrans/FormulaireProjet.tsx` + `ecrans/formulaire/{Champ,Commande,Commandes,Choix*,Groupe,contexte}` ; `Valeurs`, `valider`, `versSaisie` et `construireProjet` inchangés. Glossaire `textes/glossaire.ts` (`CodeTerme`, `GLOSSAIRE`, `texteDuTerme`), `Descripteur.terme`, `ChampHypothese` avec ⓘ hors du `<label>`, composant `Terme` ; `Info` nommé par un texte masqué (plus d'`aria-label`). Client `ClientWorker.communes(recherche, signal)` et `enrichissement/communes.ts` (lecture « 69003 Lyon », options, choix automatique). Tests : `tests/{saisie-logique,saisie-composants,combobox,glossaire,verifier,verifier-ecran}.test.*`, aide `tests/aides-verifier.ts`, aide e2e `ouvrirGroupe`.

## `apps/worker` (socle livré)

Voir `architecture/worker-socle.md`. Hono 4 sur Cloudflare Workers (`wrangler.toml` : KV `KV_CACHE`, binding Rate Limiting `LIMITEUR` 60/min/IP, `[observability]`), dépendances injectées (`creerApp(deps)`), `GET /health`, `GET /proxy/:service` avec liste blanche de services (`services/`), cache KV par empreinte des paramètres validés, réponses amont validées et normalisées au contrat Loupe, erreurs en codes. Premier service : géocodage Géoplateforme. `POST /extract` : lecture du texte d'une annonce par un modèle de langage (connecteur « chat completions », OpenRouter par défaut), 22 champs validés un par un, cache 30 jours (voir `architecture/extraction-llm.md`). `GET /marche` : médiane DVF, loyer ANIL et zone ABC d'une commune, lus sur R2 (binding `DONNEES`, bucket `deklic-data` en juridiction UE) et assemblés par une fonction pure (voir `architecture/enrichissement-marche.md`). `GET /marche/adresse` : ventes du CSV de la commune classées autour d'une adresse précise (même parcelle, parcelles voisines via l'API Carto cadastre de l'IGN, même côté et en face par code de voie et parité, cercles de 100 à 300 m) par un moteur pur sans modèle de langage ; les CSV DVF publiés par `data/` portent pour cela `idParcelle, numero, suffixe, codeVoie, voie, carrez` (voir `architecture/dvf-adresse.md`) ; chaque vente y est ramenée au dernier semestre publié grâce à `dvf/<millésime>/tendance/<dep>.json` (série de la commune ou du département, lissée), et l'estimation du bien est calculée ensuite par le moteur (`packages/moteur/src/estimation/`, voir `architecture/estimation-prix.md`). Les ventes des communes voisines (huit points à 300 m situés par API Géo) comptent dans les cercles. Services du proxy `dpe` (base ADEME des DPE à 30 m) et `risques` (rapport Géorisques), affichés et appliqués par l'onglet Estimation (voir `architecture/marche-complet.md`). Service `communes` (API Géo, Worker 0.11.0) : code postal xor nom, `Service.enCache(parametres)` pour ne garder que les recherches par code postal (voir `architecture/formulaire-rapide.md`). Feature `adresse-suggestions` (Worker 0.12.0, voir `architecture/adresse-suggestions.md`) : service `adresses` (Géoplateforme en autocomplétion, `enCache: () => false`, limiteur dédié `LIMITEUR_SUGGESTIONS` 120/min/IP choisi par chemin sous `/proxy/*`, repli sur `LIMITEUR` sans le binding) et `GET /marche/adresses-dvf` (adresses du cadastre d'une commune regroupées depuis son CSV DVF — numéro, suffixe, code de voie, voie, parcelles, point moyen des ventes situées, nombre de ventes — filtrées par le texte normalisé ; liste gardée 24 h en KV par commune). Tests Vitest en Node via `app.request()` et doubles ; couverture 100 % sur `src/**`. Build = `wrangler deploy --dry-run`.

Feature `estimation-confiance` (PR 1) : voir `architecture/estimation-confiance.md`. Moteur : `estimation/confiance.ts` (`confianceEstimation`, `interpolerPaliers`, `precisionDe`, `dispersionDe`), règles `estimation.confiance` et `estimation.marges` à cinq niveaux, `DvfSchema` + `precision`, `ancienneteMedianeMois`, `periode`, `lieu`. Data : `dateMediane` dans `StatistiquesTypeSchema`. Worker 0.7.0 : `donnees/anciennete.ts` (`moisEntre`, `milieuDePeriode`), `/marche` contrat v2, `/marche/adresse` contrat v4. Web : `textes/confiance.ts`, `ecrans/adresse/{Confiance,Repere}.tsx`, `enrichissement/{marche,adresse}.ts` (`precisionDuGroupe`), contrats tolérants à l'ancien Worker.

Feature `estimation-ventes` (fiche 14) : voir `architecture/estimation-ventes.md`. Data : `VenteSchema` + `dependances`, `terrain`, `lots` (colonnes finales du CSV DVF). Worker (contrat `/marche/adresse` v7) : `adresse/ventes-proches.ts` (plafond 300, `ventesProchesTotal`, `ventesProchesTronquees`, `carrez`, `parcelle`, `cleBan`), `adresse/dpe-ventes.ts` (`cleBanVente`, `rapprocherDpe` pur, `lireDpeVentes` : `identifiant_ban_in` par paquets de 50, 4 paquets au plus, KV 7 jours ; `dpeVentes` dans la réponse ; analyse non mise en cache si un paquet échoue). Web : `enrichissement/repere.ts` (`decisionRepere`, `appliquerRepere`, `instantaneRepere`, `restaurerRepere`), `enrichissement/ventes.ts` (`trierVentes`, `triSuivant`, `filtrerVentes`, `pageDe`), `textes/{ventes,repere}.ts`, `ecrans/adresse/{LigneAdresse,LigneRepere,RepereAnalyse,BlocRepere,TableauVentes,DetailVente}.tsx`, `CarteEstimation({ repere })`, `CONTRAT_ADRESSE` = 7 ; `Repere.tsx` supprimé.

Feature `liens-hypotheses` (fiche 17, web seul) : voir `architecture/liens-hypotheses.md`. `hypotheses/liens.ts` (tables des chiffres liés par volet, type `CheminLie`, volet porteur, adresse `{ pathname, hash, state.depuis }`, fragment, origine, repli), `hypotheses/effet.ts` (effet d'une modification), `textes/liens.ts`, `composants/ValeurHypothese.tsx`, `coque/champ-cible.ts` (`[data-champ]`, `[data-colle]`, `.mise-en-evidence`), `coque/contenu.ts` (position par entrée d'historique, `positionAuRetour`), `coque/defilement.ts` (`defilementPourCentrer`), `ecrans/hypotheses/{Retour,UtilisePar}.tsx`. Aucun état persistant, aucun réseau ; logique pure couverte à 100 %.

Feature `carte-interactive` (fiche 15) : voir `architecture/carte-interactive.md`. Web seul. Pur (couverture 100 %) : `enrichissement/carte.ts` (fonds WMTS `ORTHOIMAGERY.ORTHOPHOTOS` en JPEG et `CADASTRALPARCELS.PARCELLAIRE_EXPRESS` style `PCI vecteur`, `cleVente`), `carte-couleurs.ts`, `carte-groupes.ts`, `carte-liaison.ts` (réducteur partagé carte ↔ tableau), `carte-gestes.ts`, `textes/carte.ts` (`ficheVente`, `legendeCouleurs`). Écrans : `ecrans/adresse/{useLiaisonVentes,calques-carte,bulle-vente}.ts`, `{CarteQuartier,CarteVentes,BarreCarte,LigneVente,FiltresTableauVentes}.tsx` ; `TableauVentes` reçoit `liaison` (filtres contrôlés). Gestes sans greffon Leaflet : molette arrêtée en phase de capture React, `dragging` coupé au doigt hors plein écran (`touch-action: pan-x pan-y` de Leaflet). Plein écran en surcouche CSS (pas d'API Fullscreen). Aucun `innerHTML` : bulle et pastille chiffrée construites en DOM. Aucune dépendance ajoutée (pas de `leaflet.markercluster`).

Feature `hypotheses-commandes` (fiche 24) : voir `architecture/hypotheses-commandes.md`. Web seul. Pur (100 %) : `hypotheses/commandes.ts` (`commandeDe`, `texteNuits`, `libelleTuile`). `composants/saisie/EnveloppeChamp.tsx` partagé par `ecrans/formulaire/Champ.tsx` et `ecrans/hypotheses/ChampHypothese.tsx` ; `composants/saisie/{SaisieAnnee,SaisieApport,SaisieDuree}.tsx` ; `ecrans/hypotheses/CommandeHypothese.tsx`. Noms des boutons radio = chemin + `useId` (deux offres du simulateur dans la page). `coque/champ-cible.ts` vise la tuile cochée d'abord.

## `apps/comptes` (livré, feature comptes)

Voir `architecture/comptes.md` et `adr/006-comptes-better-auth.md`. Better Auth 1.7 sur Hono, servi sur l'origine du site par le worker Cloudflare Pages (le build de `apps/web` dépose `dist/_worker.js` si `DEKLIC_COMPTES=1` avec `dist/_routes.json`, qui n'y envoie que `/api/*`). `src/index.ts` (gestionnaire Pages : `/api/*` vers l'application, le reste vers `ASSETS`, configuration incomplète : 503), `app.ts` (santé, fournisseurs, garde puis Better Auth), `garde.ts` (hôte connu, liste blanche des routes, code de connexion seulement), `auth.ts` (options, instance par origine, envoi du code, journal sans adresse e-mail), `sociaux.ts` et `fournisseurs.ts` (Google, Apple, secret JWT ES256), `courriel.ts` (Resend, journal en dev), `dependances.ts` (Zod, production par défaut). Base D1 `deklic-comptes` : `migrations/0001_comptes.sql` générée par `scripts/generer-migration.ts` et vérifiée par un test (schéma identique, parcours complet à travers une D1 simulée sur `node:sqlite`). Tests Vitest (Node) via `app.request()` avec base mémoire, envoyeur mémoire et jarre à cookies ; couverture 100 % sur `src/**`. Build = `wrangler deploy --dry-run`. Web : `src/compte/` (client fetch + Zod, client mémoire, `CompteProvider`), écrans `Connexion.tsx`, `Compte.tsx`, `coque/Profil.tsx`.

## Gestion locative (livrée, features gerer-socle, quittances-fiches, gerer-biens, gerer-parcours, gerer-bail-revision et quittances-auto)

Voir `architecture/gerer-socle.md` (ADR-G1 à G7) et `specs/gestion-locative-specs.md` (épic G1 à G5).

- `packages/gestion` (`@loupe/gestion`) : TypeScript pur, Zod seul. `dates.ts` (jours `AAAA-MM-JJ` et mois `AAAA-MM`, jamais de fuseau), `regles.ts` (types de bien et de location, jour du loyer 1 à 28, retard à 5 jours, dépôt par défaut 1 mois nu / 2 mois meublé, montant ≤ 100 000 €), `schemas.ts` (nouveaux et enregistrés : bien, locataire, location, paiement ; `CreationLocationSchema` ; `EtatGestionSchema` ; préférences du menu avec au moins une section), `loyers.ts` (`loyerDuMois` avec prorata au jour, `suivreLoyer`), `mois.ts` (`resumeDuMois`, lignes triées par statut puis échéance puis nom). Montants en centimes entiers ; loyers dus déduits des locations, jamais stockés.
- `apps/comptes/src/gestion/` : `routes.ts` (`routeurGestion` sous `/api/gestion` : `GET /etat`, `POST /locations`, `POST /paiements`, `DELETE /paiements/:id`, `PUT /preferences` ; `bodyLimit` 64 Ko ; codes métier → 404, 409, 400 ; table absente → 503 `GESTION_INDISPONIBLE`), `acces.ts` (hôte connu, `Origin` exigé pour écrire, session Better Auth, `no-store`), `depot.ts` (interface `DepotGestion`, erreurs `INTROUVABLE`, `PERIODE_DEJA_RECUE`, `HORS_LOCATION`, `LIMITE_ATTEINTE`), `depot-d1.ts` (requêtes préparées filtrées par `userId`, création atomique par `batch`, 200 biens par compte, période payée dans la location et au plus 366 jours à l'avance), `lignes.ts` (lignes SQL → objets). Migration `0002_gestion.sql` (`gestion_bien`, `gestion_locataire`, `gestion_location`, `gestion_paiement` unique par location et mois, `gestion_preference`, clés étrangères en cascade vers `user`) appliquée par `scripts/migration.ts` (serveur Node et tests). Tests par `app.request()` sur une D1 simulée sur `node:sqlite` avec clés étrangères réelles.
- `apps/web` : `src/gestion/` (`GestionProvider` / `useGestion` : état chargé quand le compte est connecté, actions qui tiennent l'état à jour, préférences optimistes avec retour arrière ; `clientGestionReseau` revalidé par Zod, `clientGestionMemoire` pour les tests ; `menu.ts` avec copie locale `deklic.menu.v1` ; `saisie.ts` et `depuis-projet.ts` pour les deux portes ; `format.ts`), `coque/SectionAnalyser.tsx` et `SectionGerer.tsx` dans la zone qui défile de la barre latérale, `ecrans/gerer/` (`Gerer`, `SansCompte`, `Portes`, `LoyersDuMois`, `AjouterMain`, `PretAGerer`), `ecrans/compte/MonMenu.tsx`, textes `textes/gerer*.ts`. `AppEnMemoire({ gestion })`. La règle des deux clics est prouvée par des tests de rendu qui comptent les clics. Couverture 100 % sur `src/gestion/**` et les textes.
- **G1b, feature `quittances-fiches`** (`architecture/quittances-fiches.md`, ADR-G8 à G13) : `packages/gestion` ajoute le statut `partiel` et `resteDu`, `montantAcceptable` (`loyers.ts`), `documents.ts` (schémas du contenu figé, `cleDocument`, `numeroDocument`), `contenus.ts` (`contenuQuittance`, `contenuRecu`, mentions en codes), `baux.ts` (`refusFin`, `chevauche` au même libellé, `NouvelleOccupationSchema`, `OccupationCreeeSchema`), `libelle` et `colocataireIds` sur la location. `apps/comptes/src/gestion/` : `depot-documents.ts` (bailleur, émission idempotente par clé avec `on conflict do nothing`, lecture, export), `depot-baux.ts` (fin de location, louer, 50 locations par bien), `ecritures.ts` (locataire, colocataires et location écrits par `batch`), paiement par insertion conditionnelle (somme du mois ≤ dû), annulation refusée dès qu'un document existe ; routes `PUT /bailleur`, `POST /documents`, `GET /documents/:id`, `POST /locations/:id/fin`, `POST /biens/:id/locations`, `GET /export`. Migration `0003_gestion_documents.sql` : `gestion_paiement` recréée sans l'unicité par mois (copie à l'identique), `gestion_bailleur`, `gestion_document` (`unique (userId, cle)`), `gestion_colocataire`, colonne `gestion_location.libelle`. `apps/web` : `ecrans/gerer/{Loyers,LigneDeLoyer,EnPartie,IdentiteBailleur,RetoursLoyer,EcranAttente,ImprimerDocument,DocumentLoyer,FicheBien}.tsx`, `fiche/`, `useActionsLoyer.ts`, `gestion/{loyers-page,fiche,saisie-louer,memoire-documents}.ts`, `ecrans/compte/MesDonneesGestion.tsx`, textes `gerer-{loyers,bailleur,documents,fiche,louer}.ts`. Couverture 100 % sur `src/gestion/**` et les textes ; quatre écrans de plus dans `e2e/formats.ts`.

- **G1c, feature `gerer-biens`** (`architecture/gerer-biens.md`, ADR-G14 à G18) : `packages/gestion` ajoute `montants.ts` (`montantsDuMois`, `premierMoisModifiable`, `moisModifiables`, `changementRefuse`, `avecChangement`), `ChangementSchema` et `ModificationLocationSchema`, `apl` et `changements` sur la location, `apl` et `partLocataire` sur le loyer dû, `apl` facultatif dans le contenu des documents, `APL_QUITTANCE` (à confirmer), `ajouterMois`. `apps/comptes/src/gestion/` : `lecture.ts` (une location avec colocataires et changements, ses paiements), `depot-modifications.ts` (changement par insertion conditionnelle avec `on conflict (locationId, aPartirDe)`, 409 `PERIODE_PAYEE` ; jour, dépôt et libellé contrôlé par `chevauche` ; suppression d'un bien en un `batch` : le bien et sa cascade, puis les locataires sans location ; locataire corrigé), routes `PATCH /locations/:id`, `DELETE /biens/:id`, `PATCH /locataires/:id` ; paiements et documents calculés avec les changements et l'APL. Migration `0005_gestion_changements.sql` : `gestion_changement` (clé `locationId, aPartirDe`, cascade), colonne `gestion_location.apl` (0 par défaut), sans reprise de données. `apps/web` : `gestion/{biens,locataires,saisie-modifier,memoire-modifications}.ts`, `ecrans/gerer/{MesBiens,MesLocataires,ModifierLocataire}.tsx`, `fiche/{ModifierLocation,SupprimerBien}.tsx`, textes `gerer-{biens,locataires}.ts`, champ APL dans `saisie.ts` et `saisie-louer.ts`, client réseau (`PATCH`, `DELETE`), contexte (`modifierLocation`, `supprimerBien`, `modifierLocataire`). Couverture 100 % sur `src/gestion/**` et les textes ; quatre écrans de plus dans `e2e/ecrans-gerer.ts`.

- **G1d, feature `gerer-parcours`** (`architecture/gerer-parcours.md`, ADR-G19 à G23) : web seul, aucune route ni migration. `apps/web/src/gestion/` : `parcours.ts` (adresses `lienFicheBien`, `lienFicheLocataire`, `lienNouveauLocataire`, `lienLoyers`, `lienDocument` ; `retourValide`, chemin de Gérer seulement ; `destinationRetour` ; `locationCreee` lu dans l'état de navigation), `fiche-locataire.ts` (`ficheDuLocataire`), `fiche.ts` (`friseDesLocations`), `saisie-nouveau-locataire.ts` (`choixDesBiens`, `bienInitial`, `apresChangementDeBien`), `a-faire.ts` (`actionsAFaire`, `cleAction`). Écrans : `ecrans/gerer/{FicheLocataire,NouveauLocataire,AFaire,FilAriane,NomsDeLocataires,LocationCreee}.tsx`, `locataire/CarteOccupation.tsx`, `nouveau/FormulaireLouer.tsx` (ancien `fiche/LouerBien.tsx`, liste des biens par `MenuChoix`) ; liens ajoutés dans `LigneDeLoyer` (noms, montant vers `?modifier=`), `CarteLocation`, `FriseMois` (mois vers `?mois=&bien=`), `MesBiens` (« Louer »), `MesLocataires` (« Ajouter un locataire »), `Loyers` (`aria-current` sur la ligne du bien), `ImprimerDocument` (`?retour=`), `useActionsLoyer` ; textes `gerer-{parcours,locataire,a-faire}.ts`. Couverture 100 % sur `src/gestion/**` et les textes ; deux écrans de plus dans `e2e/ecrans-gerer.ts` et le parcours `e2e/gerer-parcours.spec.ts`.

- **Dépenses, prêt et Argent, feature `gerer-depenses-argent`** (`architecture/gerer-depenses-argent.md`, ADR-G24 à G28) : `packages/gestion` dépend désormais de `@loupe/moteur` ; `regles-argent.ts` (catégories, fréquences, 2 000 dépenses, bornes du prêt), `depenses.ts` (`NouvelleDepenseSchema`, `DepenseSchema`, `PretBienSchema`, `PretEnregistreSchema`, `EtatArgentSchema`), `pret.ts` (`tableauDuPret` sur `tableauAmortissement`, `echeanceDuMois`, `capitalRestantDu`, `mensualiteDuPret`, `finDuPret`), `argent.ts` (`occurrenceDuMois`, `argentDuMois`, `argentDeLAnnee`, `argentDesDerniersMois`, `courbeDesMois`, `cashflowReel` ; loyers à la date du paiement). `apps/comptes/src/gestion/argent/` : `routes.ts` (sous-routeur monté dans `routeurGestion`, son propre `onError` : 503 `DEPENSES_INDISPONIBLE`), `depot.ts`, `depot-d1.ts` (écritures conditionnelles `returning *` : limite du compte, bien du compte ; prêt en `insert … select … where exists … on conflict`), `lignes.ts` ; `Dependances.argent`. Migration additive `0007_gestion_depenses.sql`. `apps/web` : `gestion/argent/` (`ArgentProvider` / `useArgent`, clients réseau et mémoire, `saisie-depense.ts`, `saisie-pret.ts`, `pret-analyse.ts` via `calculerFinancement`, `page.ts`), écrans `ecrans/gerer/argent/`, textes `gerer-argent.ts`, `AppEnMemoire({ argent })`. Couverture 100 % ; trois écrans de plus dans `e2e/ecrans-gerer.ts` et le parcours `e2e/argent.spec.ts`.
- **B1, feature `gerer-bail-revision`** (`architecture/gerer-bail-revision.md`, ADR-G29 à G28) : `packages/gestion` ajoute `regles-bail.ts` (IRL en centièmes avec dates de publication et source INSEE, décence énergétique métropole et outre-mer, validité du DPE, gel F et G, prévenances d'un mois), `bail.ts` (schémas `LegalBien*`, `Revision*`, `DemandeRevision` ; `publicationIrl`, `trimestrePublieA`, `anniversaireCourant`, `revisionParDefaut`, `moisDEffet`, `propositionRevision` : une seule année d'IRL comparée, arrondi au centime), `conformite.ts` (`classeDpeDuProjet`, `finValiditeDpe`, `dateInterdiction`, `alertesConformite`, `estUrgente`), `lettre.ts` (contenu figé, `cleLettre`, `numeroLettre`, `EtatBailSchema`, `RevisionAppliqueeSchema`). `apps/comptes/src/gestion/bail/` : `routes.ts` (monté d'une ligne dans `routeurGestion` : garde, `Origin` et 64 Ko hérités ; 503 `BAIL_INDISPONIBLE` si une table de 0008 manque), `depot.ts`, `depot-d1.ts` (lot D1 changement + lettre + révision, chacun `where not exists (paiement ≥ mois d'effet)`), `lignes.ts` ; dépendance `bail` dans `Dependances`. Migration `0008_gestion_bail.sql` uniquement additive. `apps/web` : `gestion/bail/` (client réseau et mémoire aux mêmes règles, `clientBailIndisponible` par défaut d'`AppEnMemoire`, `BailProvider` / `useBail`, `vue.ts`, `saisie.ts`), `GestionContext.integrerLocation`, `ecrans/gerer/bail/{CarteConformite,ModifierConformite,CarteRevision,ReglagesRevision,LettreRevision,ImprimerLettre}.tsx`, `a-faire.ts` (types `alerte` et `revision`), `parcours.ts` (`lienLettre`, `lienConformite`, `lienRevision`), textes `gerer-bail.ts`. Deux écrans de plus dans `e2e/ecrans-gerer.ts`, parcours `e2e/gerer-bail.spec.ts`.
- **G2-1, G2-2, feature `quittances-auto`** (`architecture/quittances-auto.md`, ADR-G40 à G46) : `packages/gestion/src/envois.ts` (statuts d'accord enregistrés et effectifs, `statutAccord` lié à l'empreinte de l'adresse, `accordValide`, `destinatairesQuittance`, `masquerEmail`, `invitationPossible`, `renvoiPossible`, `expirationJeton`, `locatairesAVerifier`, schémas `EtatEnvoisSchema`, `ContactLocataireSchema`, `BailleurBienSchema`, `JetonAccordSchema`, `LectureAccordSchema`, `ReponseAccordSchema` ; `REGLES_ENVOIS` : 30 jours, 2 tentatives, 12 s, 1 min, 24 h ; `ENVOI_DEMATERIALISE` art. 21 vérifié). `apps/comptes` : `courriel.ts` (pièces jointes et `reply_to` Resend, `Envoyeur.mode`, statut HTTP dans `ErreurCourriel`), `courriels/{format,document,gabarits,pdf,pdf-quittance}.ts` (gabarits HTML échappé + texte testés par instantané, écrivain PDF ASCII Helvetica WinAnsi avec xref exacte), `gestion/envois/{jetons,depot,depot-d1,taches,quittances,declencheurs,routes,public}.ts` (HMAC SHA-256 vérifié à temps constant, jetons à usage unique par écriture conditionnelle, intergiciel qui confie invitations et quittances à `executionCtx.waitUntil`, table absente → 503 `ENVOIS_INDISPONIBLE`), `depot-documents.ts` lit l'identité du bien en tolérant la table absente ; `Dependances.{envois,jetons,attendre}`, variable `JETON_COURRIEL_SECRET`. Migration additive `0009_gestion_envois.sql` : `gestion_locataire_contact`, `gestion_bien_bailleur`, `gestion_accord`, `gestion_jeton`, `gestion_envoi` (unique `documentId, locataireId`), toutes en cascade. `apps/web` : `gestion/envois/{types,reseau,memoire,logique,EnvoisContext}` (`EnvoisProvider` / `useEnvois`, inerte sans fournisseur), `ecrans/accord/Accord.tsx` (hors coque), `ecrans/gerer/envois/{CarteAccord,TraceEnvoi,BailleurDuBien}.tsx`, téléphone dans `ModifierLocataire` et `FormulaireLouer`, types `email_a_verifier` et `accord` dans `a-faire.ts`, message après « Reçu » dans `useActionsLoyer`, textes `gerer-envois.ts`. Trois écrans de plus dans `e2e/ecrans-gerer.ts`.
- **A2, feature `gerer-reel-declaration`** (`architecture/gerer-reel-declaration.md`, ADR-G34 à G38) : aucune table, aucune route, migration 0010 laissée libre. `packages/gestion` : `reel-prevu.ts` (`projetDuBien` par `migrerProjet` + `ProjetSchema`, `regimeDeLAnalyse`, `prevuDuBien` sur `calculerProjet(…, { avecScenarios: false })` : postes en centimes par mois, lignes de charges du moteur rangées par catégorie de Gérer, charges refacturées ajoutées aux loyers et à la copropriété ; `moisDeComparaison`, `reelMensuel`, `comparerReelPrevu`), `declaration.ts` (`recettesDeLAnnee` à la date des paiements et au prorata de `montantsDuMois` ; `declarationDeLAnnee(donnees, annee, regles = obtenirRegles(VERSION_REGLES_COURANTE))` : groupes vide et meublé, `repartirResultat` selon les lignes 430 à 442 de la notice, intérêts et assurance des prêts par `echeanceDuMois`), `regles-declaration.ts` (numéros de lignes et de cases, forfait de 20 € par local, abattement minimum de 305 €, sources datées), `export-annee.ts` (`lignesDeLAnnee`). `apps/web` : `gestion/declaration/{page,csv,analyser}.ts`, `ecrans/gerer/declaration/{Declaration,BlocsDeclaration,RecapitulatifAnnee}.tsx`, `ecrans/gerer/argent/CarteReelPrevu.tsx` (fiche du bien), `NouveauProjet.tsx` lit l'état de navigation `{ depuisGestion }`, `Compte.tsx` (export avant suppression), `parcours.ts` (`CHEMIN_DECLARATION`, `lienDeclaration`, `lienRecapitulatif`), menu « Déclaration », textes `gerer-declaration.ts`, téléchargement par `ecrans/simulateur/telecharger.ts`. Deux écrans de plus dans `e2e/ecrans-gerer.ts`, parcours `e2e/declaration.spec.ts`.

## Synchronisation des projets (livrée, feature sync-projets)

Voir `architecture/sync-projets.md` (ADR-S1 à S7).

- `packages/projets` (`@loupe/projets`) : `enregistre.ts` (schémas du projet enregistré déplacés du web et réexportés par `apps/web/src/stockage/projets.ts`, `migrerEnregistre`, `NOM_EXEMPLE`, `estExempleIntact`), `contrat.ts` (bornes, `ChangementSchema`, `RequeteSynchroSchema`, `lireReponse` qui écarte un projet illisible sans bloquer les autres), `journal.ts` (`JournalSynchroSchema` : compte, curseur, première lecture, projets synchronisés, à envoyer, à supprimer ; `noterEnregistrement`, `noterSuppression`), `fusion.ts` (`changerDeCompte`, `oublierCompte`, `detacherCompte`, `preparerEnvoi`, `appliquerReponse`). Dépend de `@loupe/moteur` et de Zod.
- `apps/comptes/src/projets/` : `routes.ts` (`POST /api/projets/synchroniser`, `bodyLimit` 720 Ko, table absente → 503 `PROJETS_INDISPONIBLE`), `depot.ts` (interface `DepotProjets` qui rend le JSON déjà assemblé), `depot-d1.ts` (lecture des lignes visées, limite de 200 actifs, lot atomique d'upserts « dernière modification gagne » avec révision `max + 1` dans la même instruction, purge des suppressions de plus de 90 jours ou au-delà des 500 plus récentes sans jamais toucher la révision maximale, page de 25 par révision, versions du compte pour les envois perdus, liste des actifs ; contenu recopié sans être réanalysé). Garde de session partagée `src/acces.ts` (réexportée par `gestion/acces.ts`). Migration `0004_projets.sql` (clé `(userId, id)`, index `(userId, revision)`, cascade vers `user`). Tests par `app.request()` sur la D1 simulée.
- `apps/web` : `stockage/ProjetsContext.tsx` tient `{ projets, journal }` dans une référence (deux actions du même clic se voient), note chaque création, modification et suppression au journal (`stockage/journal.ts`, `loupe.synchro.v1`) et expose `useStockageProjets` (lire, transformer, version locale) ; l'exemple n'est pas amorcé sur un appareil lié à un compte. `stockage/synchro/` : `types.ts`, `reseau.ts` (`clientProjetsReseau`, codes), `memoire.ts` (`clientProjetsMemoire`, mêmes règles que l'API), `cycle.ts` (`synchroniserTout` : lots, pages, 40 échanges au plus), `SynchroContext.tsx` (`SynchroProvider` sous `CompteProvider`, `useSynchro().statut`). `CompteContext` expose `sortie` (`deconnexion` ou `suppression`). Ligne d'état `textes/synchro.ts` dans Mes projets et Mon compte. `AppEnMemoire({ projets })` avec envoi sans délai. Couverture 100 % sur `stockage/**` et les textes ; la spec des formats simule le compte des projets (`e2e/formats.ts`).

## `packages/capture` et `apps/extension` (livrés, feature extension)

Voir `architecture/extension.md`. `@loupe/capture` : `CaptureSchema` (version 1, portail, url, champs optionnels bornés, description ≤ 4 000 caractères, captureLe, mode, regles), `encoderCapture` / `decoderCapture` (base64url d'un JSON UTF-8, jamais d'exception), `urlDeCapture` / `captureDepuisHash` (`#capture=…`), `resoudreAnnonce` (une seule source de vérité pour les cinq portails, ré-exportée par le web), moteur de règles (`ReglesPortailSchema`, extracteurs `jsonld` / `json` / `meta` / `css` avec regex, `valeur`, `diviser`, conversions, `appliquerRegles`, `capturer`, `creerRegistre`). Import d'espace de noms de Zod pour des bundles légers. `@loupe/extension` : Manifest V3 (`activeTab` + `scripting`, rien d'autre), `regles/<portail>.json` versionnés `<portail>-AAAA-MM-JJ` et exportés, popup et script de contenu bâtis par esbuild (`node --experimental-strip-types scripts/build.ts [--dev] [--watch]`) vers `dist/chrome` et `dist/firefox`, icônes PNG dessinées au build. Web : `src/annonces/capture.ts`, `src/bookmarklet/` (bouton-favori construit par `vite.bookmarklet.config.ts` dans `public/capture.js`, ignoré par git), page `/extension`. Couverture 100 % exigée sur `packages/capture/src/**`, `apps/extension/src/**` et `apps/web/src/bookmarklet/**`. Fixtures HTML dans `apps/extension/tests/fixtures/` (hors Prettier).

### Lecture automatique d'un lien collé (feature lecture-auto)

Voir `architecture/lecture-auto.md`. `@loupe/capture` : `pont.ts` (messages page ↔ extension validés par Zod : `ping`/`pong`, `lire`/`resultat`, raisons `hors-annonce`, `portail-sans-regles`, `permission`, `chargement`, `vide`, `occupe`), source d'extracteur `donnees` et `donnees.url` relative au portail, `capturerAvecDonnees` avec chargeur injecté, `analyserJson` qui lit aussi `window["X"]=JSON.parse("…")`, champs `typeBien`, `lotsCopro`, `coproEnProcedure`. `@loupe/extension` 0.2.0 : `pont.ts` (script de contenu des pages Deklic, `document_start`), `arriere-plan.ts` (service worker Chrome, script Firefox) branché sur `logique/lecteur.ts` (orchestration testée sans `chrome.*`), permissions d'hôte des cinq portails, bouton d'autorisation dans le popup. Web : `annonces/extension.ts` (détection 500 ms, lecture 90 s), `ecrans/nouveau-projet/LectureAuto.tsx` (hook + état), `enrichissement/lecture.ts` `completerAvecIa`, formulaire enrichi (type, GES, copropriété). Tests de l'écran avec l'extension simulée par `vi.mock('@/annonces/extension')`.

### Lecture par le serveur (feature lecture-serveur)

Voir `architecture/lecture-serveur.md` et `adr/008-lecture-serveur.md`. `@loupe/capture` : champs enrichis optionnels (version 1 inchangée), `FicheAnnonceSchema`/`CHAMPS_FICHE`, `PhotosCaptureSchema` (https, ≤ 30, ≤ 500 caractères), mode `serveur`, chemins `[*]` (projection aplatie), tableaux de valeurs simples lus comme texte joint, types `urls` et `date`, export `./portails`. Worker 0.10.0 : `lecture/route.ts` (`lireDemande` : origine, taille, schéma, liste blanche https ; `lireParFournisseur` : 2 tentatives, marqueurs par portail, 75 s), `lecture/fournisseur.ts` (`LecteurPages`, `lecteurBrightData` : `POST https://api.brightdata.com/request { zone, url, format: raw, country: fr }`, statut du portail lu dans `x-brd-status-code`, taille ≤ 3 Mo vérifiée avant et après lecture), `lecture/bienici.ts`, binding `LIMITEUR_LECTURE`. Web : `ClientWorker.lirePage(url, signal)` (`signalDeRequete` combine délai 160 s et annulation), `ReponseLectureSchema`, `annonces/{lecture-serveur,attente,fiche,regles}.ts`, `ecrans/nouveau-projet/{useLectureAutomatique.ts,AttenteLecture.tsx,LectureAuto.tsx}`, `ecrans/rapport/CarteBien.tsx`, textes `lecture-serveur.ts`, `attente.ts`, `fiche-bien.ts`, `ProjetEnregistre.annonce`. Tests : Worker par `app.request()` avec lecteur et fournisseur simulés ; règles sur fixtures réelles anonymisées ; écrans par `AppEnMemoire({ client })` avec `lirePage` simulé ; aucun appel réseau.

## `data/` (livré, feature referentiels)

Voir `architecture/referentiels.md` et `data/SOURCES.md`. Workspace `@loupe/data` exécuté directement par Node (`node --experimental-strip-types src/cli.ts`, imports en `.ts`, aucun build) : `commun/` (CSV en flux RFC 4180, décodage UTF-8 / Windows-1252, téléchargement avec trois tentatives et gzip, journal JSON + annotations GitHub, quartiles, dates, 101 départements), `schemas/` (Zod : un schéma par fichier publié, en-tête `genereLe` / `millesime` / `source`), `sources/<source>/` (constantes, transformation pure, orchestration), `cli/arguments.ts`, `sources/executer.ts`. Tout accès externe passe par un `Contexte` injecté (`fetch`, pause, horloge, journal, dossier de sortie) : les tests utilisent un faux `fetch` nourri par des extraits réels (`tests/fixtures/`), sans réseau. Couverture 100 % exigée sur `data/src/**` (hors `cli.ts`). Sortie `data/dist/` (ignorée par git), publiée sur R2 `deklic-data` par `.github/workflows/referentiels.yml` (`aws s3 sync`, secrets `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `CLOUDFLARE_ACCOUNT_ID`). Seuils de l'usure saisis à la main dans `data/sources/usure/`.

## `apps/site` (livré, feature site-vitrine)

Voir `architecture/site-vitrine.md` et `adr/010-site-vitrine.md`. Site vitrine et guides de `deklic.pro`, workspace `@loupe/site`. Astro 7.3 (Vite 8, compilateur Rust strict sur le HTML, Markdown natif Sätteri), sortie statique : `trailingSlash: 'always'`, `build.format: 'directory'`, `build.inlineStylesheets: 'never'`, `vite.build.assetsInlineLimit: 0` (aucun style ni script inline, exigé par la CSP). Intégrations : `@astrojs/mdx`, `@astrojs/sitemap` (sans la 404), `src/integrations/verification.ts` (`astro:build:done` : `verifierPage` sur chaque HTML, puis `dist/_headers`). Tailwind v4 par `@tailwindcss/vite`, tokens repris d'`apps/web` et comparés à `marque/couleurs.json` par un test ; polices `@fontsource-variable/{outfit,nunito-sans}` ; aucune intégration React (le simulateur est un script TypeScript qui importe `@loupe/moteur`). Logique pure dans `src/lib/` (jamais `astro:content`, testée en Node, couverture 100 %) ; pages, composants `.astro`, intégration et scripts navigateur exclus de la couverture et prouvés par le build. Collection `guides` : `glob` sur `contenu/guides/*.mdx`, `SchemaGuide` en `astro/zod` ; `getStaticPaths` de `pages/guides/[slug].astro` appelle `verifierMotsCles` et `cheminsGuides`. Composants MDX passés par `components` : `Regle`, `Calcul`, `Simulateur`. Typecheck : `astro check` (`@astrojs/check`). Adresse de l'application : `liensApplication(process.env.DEKLIC_ORIGINE)`. Déploiement : projet Cloudflare Pages `deklic-site` (`npm ci && npm run build -w apps/site`, sortie `apps/site/dist`, chemins surveillés).

## Conventions transverses

- Zod à chaque frontière (entrée utilisateur, sortie LLM, réponse d'API, env). Types inférés (`z.infer`, `z.input`).
- Pas de `console.log` en code applicatif. Le moteur ne logue jamais ; le Worker utilisera un logger structuré.
- Erreurs : classes nommées héritant de `ErreurMoteur` ; les « pas de solution » légitimes (TRI, prix cible) rendent `null`.
- Verdict et vigilance rendent des **codes**, jamais des phrases : les textes sont dans l'interface. Les questions de visite portent leur texte dans le moteur (base sourcée, versionnée), avec des jetons `{dpe}`, `{ecart}`, `{risques}`… que l'interface formate (`textes/visite.ts`) ; le moteur ne compose jamais un montant.
- Textes utilisateur en français, sans tiret cadratin.
- Commits : `feat(moteur): US-3 — …`.

## Quality gates

1. `npm run lint` → 0 erreur, 0 warning
2. `npm run format:check` → conforme
3. `npm run typecheck` → 0 erreur
4. `npm run test:coverage` → 0 échec ; moteur 100 %
5. `npm run build` → OK
6. Aucun `TODO` / `FIXME` (règle ESLint `no-warning-comments`)
7. Aucun secret, aucun `.env` suivi, `.gitignore` à jour
8. Aucun `eval`, `dangerouslySetInnerHTML`, clé API côté client
9. `npm run test:e2e` → 8 parcours Playwright verts (en local avant la PR ; job CI `e2e`, pas encore bloquant pour le merge)

## Tests

| Type             | Outil                                                                                                                                           | Où                                     |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| Unitaires moteur | Vitest                                                                                                                                          | `packages/moteur/tests/<module>`       |
| Intégration      | Vitest                                                                                                                                          | `packages/moteur/tests/integration`    |
| Worker           | Vitest (Node) + `app.request()` et doubles                                                                                                      | `apps/worker/tests/`                   |
| Comptes          | Vitest (Node) + `app.request()`, base mémoire, D1 simulée sur `node:sqlite`                                                                     | `apps/comptes/tests/`                  |
| Référentiels     | Vitest (Node) + faux `fetch` et fixtures réelles                                                                                                | `data/tests/`                          |
| Capture          | Vitest (jsdom) : pages synthétiques via `DOMParser`                                                                                             | `packages/capture/tests/`              |
| Extension        | Vitest (jsdom) : pages enregistrées, faux `chrome`                                                                                              | `apps/extension/tests/`                |
| E2E web          | Playwright (Chromium : ordinateur, téléphone, tablette ; spec des formats 21 écrans × 9 formats ; build de production servi par `vite preview`) | `apps/web/e2e/`                        |
| Propriétés web   | Vitest, tirages pseudo-aléatoires à graine fixe (`tests/tirage.ts`)                                                                             | `apps/web/tests/*.proprietes.test.ts`  |
| Annonce témoin   | GitHub Action quotidienne (à venir)                                                                                                             | `.github/workflows/annonce-temoin.yml` |

Feature `simulateur-pret` : voir `architecture/simulateur-pret.md`. Moteur `packages/moteur/src/pret/` (schémas bornés avec messages français, `simulerPret` sur `tableauAmortissement` / `taeg` / `echeancier`, `comparerOffres`, `fraisNotaireEstimes`). Web `apps/web/src/simulateur/` (saisie texte → `versSimulation` carte par carte, `calculer`, `etatInitial`, lien `#s=` par `stockage/base64url.ts` partagé avec le partage d'un projet, mémoire `loupe.simulateur.v1`, `csvAmortissement`), écrans `ecrans/SimulateurPret.tsx`, `ecrans/simulateur/*`, `ecrans/SimulateurImprimer.tsx` (`ModeDocument`) ; seul effet hors React : `ecrans/simulateur/telecharger.ts` (Blob + lien `download`). Couverture 100 % sur `simulateur/` (glob ajouté aux seuils). e2e `apps/web/e2e/simulateur.spec.ts` (téléchargement vérifié par `waitForEvent('download')`), écrans « Simulateur de prêt » et « Simulation imprimée » dans `e2e/formats.ts`.
