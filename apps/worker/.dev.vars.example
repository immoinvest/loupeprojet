# Copier en .dev.vars (ignoré par git) pour `npm run dev -w apps/worker`.
ENVIRONNEMENT=dev
# Origines supplémentaires autorisées (séparées par des virgules) en plus de la production et de localhost:5173.
ORIGINES_AUTORISEES=
