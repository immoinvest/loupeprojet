{
  "name": "@loupe/data",
  "version": "0.1.0",
  "private": true,
  "description": "Pré-agrégation des référentiels publics de Loupe (DVF, loyers ANIL, taxe foncière REI, zonage ABC, seuils de l'usure, communes), publiés sur Cloudflare R2 par GitHub Action.",
  "type": "module",
  "scripts": {
    "referentiels": "node --experimental-strip-types src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "^4.1.0"
  }
}
